generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Nenhuma entidade configurada ainda.
// Adicione seus models aqui, ex.:
// model User {
//   id        String   @id @default(uuid())
//   createdAt DateTime @default(now())
// }
